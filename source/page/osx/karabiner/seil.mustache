<div class="container">
  <header>
    <div class="jumbotron">
      <h1>Seil</h1>

      <p>
        {{#language_en}}
          Utility for the caps lock key and some international keys in PC keyboards.
        {{/language_en}}
        {{#language_ja}}
          caps lockキーと、PCキーボードの「無変換」「変換」「ひらがな」キーのためのユーティリティ。
        {{/language_ja}}
      </p>

      <ul>
        {{#language_en}}
          <li>You can change the caps lock key to another key. (eg. escape key)</li>
          <li>You can activate some international keys in PC keyboard.</li>
        {{/language_en}}
        {{#language_ja}}
          <li>caps lockキーの動作を変更します。（例: caps lockキーをdeleteキーにする）</li>
          <li>PC用のキーボードにある「無変換」「変換」「ひらがな」などのキーをOS Xで使用可能にします。</li>
        {{/language_ja}}
      </ul>

      <p>
        {{#strip_html}}
          <a class="btn btn-primary btn-lg"
            href="files/Seil-11.3.0.dmg">
            {{#language_en}}
              Download Seil-11.3.0
            {{/language_en}}
            {{#language_ja}}
              Seil-11.3.0をダウンロード
            {{/language_ja}}
            &nbsp;
            <span class="label label-default">
              {{#language_en}}
                For OS X 10.11, 10.10, 10.9
              {{/language_en}}
              {{#language_ja}}
                OS X 10.11、10.10、10.9用
              {{/language_ja}}
            </span>
          </a>
        {{/strip_html}}
      </p>

      <p style="margin-left: 1em">
        <small>
          <a href="#history">
            {{#language_en}}
              old versions and version history
            {{/language_en}}
            {{#language_ja}}
              古いバージョンのダウンロード・更新履歴
            {{/language_ja}}
          </a>
        </small>
      </p>

      <hr />

      <div class="alert alert-info">
        {{#language_en}}
          Prior to version 10.7.0, Seil was called <b>PCKeyboardHack</b>.
        {{/language_en}}
        {{#language_ja}}
          Seilはバージョン10.7.0まで<b>PCKeyboardHack</b>という名前でした。
        {{/language_ja}}
      </div>

      <p>
        {{#strip_html}}
          <a class="btn btn-default"
            href="files/Seil-10.11.0.dmg">
            {{#language_en}}
              Download Seil-10.11.0
            {{/language_en}}
            {{#language_ja}}
              Seil-10.11.0をダウンロード
            {{/language_ja}}
            &nbsp;
            <span class="label label-default">
              {{#language_en}}
                For OS X 10.8
              {{/language_en}}
              {{#language_ja}}
                OS X 10.8用
              {{/language_ja}}
            </span>
          </a>
        {{/strip_html}}
      </p>

      <p>
        {{#strip_html}}
          <a class="btn btn-default"
            href="files/PCKeyboardHack-9.0.0.dmg">
            {{#language_en}}
              Download PCKeyboardHack-9.0.0
            {{/language_en}}
            {{#language_ja}}
              PCKeyboardHack-9.0.0をダウンロード
            {{/language_ja}}
            &nbsp;
            <span class="label label-default">
              {{#language_en}}
                For OS X 10.7
              {{/language_en}}
              {{#language_ja}}
                OS X 10.7用
              {{/language_ja}}
            </span>
          </a>
        {{/strip_html}}
      </p>
      <p>
        {{#strip_html}}
          <a class="btn btn-default"
            href="files/PCKeyboardHack-7.4.0.dmg">
            {{#language_en}}
              Download PCKeyboardHack-7.4.0
            {{/language_en}}
            {{#language_ja}}
              PCKeyboardHack-7.4.0をダウンロード
            {{/language_ja}}
            &nbsp;
            <span class="label label-default">
              {{#language_en}}
                For OS X 10.6
              {{/language_en}}
              {{#language_ja}}
                OS X 10.6用
              {{/language_ja}}
            </span>
          </a>
        {{/strip_html}}
      </p>
      <p>
        {{#strip_html}}
          <a class="btn btn-default"
            href="files/PCKeyboardHack-5.1.0.pkg.tar.gz">
            {{#language_en}}
              Download PCKeyboardHack-5.1.0
            {{/language_en}}
            {{#language_ja}}
              PCKeyboardHack-5.1.0をダウンロード
            {{/language_ja}}
            &nbsp;
            <span class="label label-default">
              {{#language_en}}
                For OS X 10.5, 10.4
              {{/language_en}}
              {{#language_ja}}
                OS X 10.5、10.4用
              {{/language_ja}}
            </span>
          </a>
        {{/strip_html}}
      </p>
    </div>
  </header>

  <div class="row">
    <div class="col-md-9 pqrs-docs-container">
      <section>
        <div class="page-header">
          <h1 id="installation">
            {{#language_en}}
              Installation
            {{/language_en}}
            {{#language_ja}}
              インストール方法
            {{/language_ja}}
          </h1>
        </div>

        <div class="row">
          <div class="col-lg-4">
            <div class="col-show-grid">
              <h2>Step 1</h2>

              <p>
                {{#language_en}}
                  Open a downloaded dmg file, and then open a pkg file in dmg.
                {{/language_en}}
                {{#language_ja}}
                  ダウンロードしたdmgファイルを開き、dmgの中のpkgファイルを開きます。
                {{/language_ja}}
              </p>
              <p>
                <img
                alt="dmg"
                {{#imgsrc}}
                  ../images/dmg-icon@2x.png
                {{/imgsrc}}
                />
                <img
                alt="pkg"
                {{#imgsrc}}
                  ../images/pkg-icon@2x.png
                {{/imgsrc}}
                />
              </p>
            </div>
          </div>
          <div class="col-lg-4">
            <div class="col-show-grid">
              <h2>Step 2</h2>

              <p>
                {{#language_en}}
                  Installer is launched. Install Seil.
                {{/language_en}}
                {{#language_ja}}
                  インストーラが起動しますので、Seilをインストールします。
                {{/language_ja}}
              </p>
              {{#lightbox}}
                img/seil-installer@2x.png installer
              {{/lightbox}}
              <div class="alert alert-warning">
                {{#language_en}}
                  Seil will be installed into /Applications folder. <br />
                  <br />
                  <b>Note:</b><br />
                  Do not change the location of Seil.app from /Applications. <br />
                  <br />
                  For example, Seil will not work properly if you moved Seil.app into /Applications/Utils.
                {{/language_en}}
                {{#language_ja}}
                  Seilはアプリケーションフォルダにインストールされます。<br />
                  <br />
                  <b>注意:</b><br />
                  アプリケーションフォルダからSeil.appを移動しないでください。<br />
                  <br />
                  例えば、/Applications/UtilsにSeil.appを移動すると、正常に動作しなくなります。
                {{/language_ja}}
              </div>
            </div>
          </div>
          <div class="col-lg-4">
            <div class="col-show-grid">
              <h2>Step 3</h2>

              <p>
                {{#language_en}}
                  Launch Seil from Launchpad.
                {{/language_en}}
                {{#language_ja}}
                  LaunchpadからSeilを起動します。
                {{/language_ja}}
              </p>

              <p>
                <img
                alt="icon"
                {{#imgsrc}}
                  img/seil-icon@2x.png
                {{/imgsrc}}
                />
              </p>
            </div>
          </div>
        </div>
      </section>

      <section>
        <div class="page-header">
          <h1 id="usage">
            {{#language_en}}
              Usage
            {{/language_en}}
            {{#language_ja}}
              使い方
            {{/language_ja}}
          </h1>
        </div>

        <h2>Step 1</h2>
        <p>
          {{#language_en}}
            Open Launchpad (or open "<span class="glyphicon glyphicon-folder-open"></span> Applications" folder from Finder).
          {{/language_en}}
          {{#language_ja}}
            Launchpadを開いてください。（もしくは、「<span class="glyphicon glyphicon-folder-open"></span> アプリケーション」フォルダをFinderで開いてください。）
          {{/language_ja}}
        </p>
        <p>
          {{#language_en}}
            Then, click Seil.
          {{/language_en}}
          {{#language_ja}}
            そして、Seilをクリックしてください。
          {{/language_ja}}
        </p>
        <div class="row">
          <div class="col-lg-4">
            <img
            alt="icon"
            {{#imgsrc}}
              img/seil-icon@2x.png
            {{/imgsrc}}
            />
          </div>
        </div>

        <br />

        <p>
          <span class="label label-info">Note</span>
          {{#language_en}}
            If you're using PCKeyboardHack 8.0.0 or earlier,
            <a href="files/preferencepane-pckeyboardhack.png">Preferences is in System Preferences.</a>
          {{/language_en}}
          {{#language_ja}}
            PCKeyboardHack 8.0.0、またはそれ以前のバージョンを使用している場合、<a href="files/preferencepane-pckeyboardhack.png">設定画面はシステム環境設定にあります。</a>
          {{/language_ja}}
        </p>

        <h2>Step 2</h2>
        <p>
          {{#language_en}}
            Activate items and change the keycode if needed.
          {{/language_en}}
          {{#language_ja}}
            有効にしたい設定にチェックを入れて、必要に応じてキーコードの変更をします。
          {{/language_ja}}
        </p>
        <div class="row">
          <div class="col-lg-4">
            {{#lightbox}}
              img/seil-preferences@2x.png Preferences
            {{/lightbox}}
          </div>
        </div>

        <h2 id="noaction">
          Step 3
          <span class="label label-info">Important</span>
        </h2>
        <p>
          {{#language_en}}
            If you are using Seil to change the behavior of caps lock,
            open "System Preferences &gt; Keyboard &gt; Modifier Keys..." and
            change caps lock configuration to "No Action".
          {{/language_en}}
          {{#language_ja}}
            caps lockキーの動作を変更するためにSeilを使っている場合は、システム環境設定の「キーボード」から「修飾キー」ボタンを押してcaps lockの動作を「アクションなし」にしてください。
          {{/language_ja}}
        </p>
        <p>
          {{#language_en}}
            You need to do this step to to reduce caps lock delay.
          {{/language_en}}
          {{#language_ja}}
            caps lockキーの遅延を抑えるためにこの設定を行う必要があります。
          {{/language_ja}}
        </p>
        <div class="row">
          <div class="col-lg-4">
            {{#lightbox}}
              img/capslock@2x.png No Action
            {{/lightbox}}
          </div>
        </div>

        <div id="noaction-multiple-devices" class="alert alert-info">
          <h3>Note:</h3>
          <p>
            {{#language_en}}
              If you are using multiple devices, you should choose devices at "No Action" configuration.
            {{/language_en}}
            {{#language_ja}}
              複数のデバイスを使っている場合、「アクションなし」の設定の際にデバイスを選択してください。
            {{/language_ja}}
          </p>
          <div class="row">
            <div class="col-lg-4">
              {{#lightbox}}
                img/seil-noaction-multiple-devices@2x.png No Action
              {{/lightbox}}
            </div>
          </div>
        </div>
      </section>

      <section>
        <div class="page-header">
          <h1 id="uninstall">
            {{#language_en}}
              Uninstall
            {{/language_en}}
            {{#language_ja}}
              アンインストール方法
            {{/language_ja}}
          </h1>
        </div>

        <ol class="steps">
          <li>
            {{#language_en}}
              Press the "Launch uninstaller" button in Seil Preferences.
            {{/language_en}}
            {{#language_ja}}
              設定画面の「Misc &amp; Uninstall」タブにある「Launch uninstaller」ボタンを押してください。
            {{/language_ja}}
            <div class="row">
              <div class="col-lg-4">
                {{#lightbox}}
                  img/seil-uninstall@2x.png Seil
                {{/lightbox}}
              </div>
            </div>
          </li>
        </ol>
      </section>

      <section>
        <div class="page-header">
          <h1 id="faq">
            {{#language_en}}
              Q&amp;A
            {{/language_en}}
            {{#language_ja}}
              Q&amp;A
            {{/language_ja}}
          </h1>
        </div>

        <h2 id="specify-device">
          {{#language_en}}
            How to change the caps lock key only in specified keyboards (retain an internal keyboard)?
          {{/language_en}}
          {{#language_ja}}
            caps lockを特定のキーボードでのみ変更するには？
          {{/language_ja}}
        </h2>

        <p>
          {{#language_en}}
            Seil changes key behavior in all keyboards. <br />
            So, you cannot specify enabled devices in Seil.
          {{/language_en}}
          {{#language_ja}}
            Seilは全てのキーボードの挙動を変更します。<br />
            そのため、Seilではデバイスを指定することは出来ません。
          {{/language_ja}}
        </p>
        <p>
          {{#language_en}}
            But you can change keys only in specified devices by using Karabiner and Seil together. <br />
            First, change the caps lock key to f19 key in Seil, and then change f19 key in Karabiner.
          {{/language_en}}
          {{#language_ja}}
            しかし、KarabinerとSeilを使うことによって特定のデバイスでのみキーを変更することが可能です。<br />
            まず、Seilでcaps lockをf19キーに変更して、f19キーをKarabinerで変更します。
          {{/language_ja}}
        </p>

        <p>
          {{#language_en}}
            Steps:
          {{/language_en}}
          {{#language_ja}}
            手順:
          {{/language_ja}}
        </p>

        <ol class="steps">
          <li>
            <p>
              {{#language_en}}
                Change key code of the caps lock key to "80" in Seil. <br />
                (Do not forget <a href="#usage">"No Action" configuration.</a>)
              {{/language_en}}
              {{#language_ja}}
                Seilでcaps lockキーのキーコードを「80」に変更します。<br />
                （<a href="#usage">システム環境設定から「アクションなし」に変更する</a>ことを忘れないでください。）
              {{/language_ja}}
            </p>
            <p>
              {{#language_en}}
                Then the caps lock key works as f19 key.
              {{/language_en}}
              {{#language_ja}}
                そうするとcaps lockキーはf19キーとして動作します。
              {{/language_ja}}
            </p>
          </li>
          <li>
            <p>
              {{#language_en}}
                Change f19 key in Karabiner with <a href="xml.html.{{ language }}#devicedef">&lt;device_only&gt; tag.</a>
              {{/language_en}}
              {{#language_ja}}
                f19キーを<a href="xml.html.{{ language }}#devicedef">&lt;device_only&gt;タグ</a>を使ってKarabinerで変更します。<br />
              {{/language_ja}}
            </p>
            <p>
              {{#language_en}}
                Please use the following private.xml with replacing &lt;vendorid&gt; and &lt;productid&gt;.<br />
                (<a href="document.html.{{language}}#privatexml">How to use private.xml.</a>)
              {{/language_en}}
              {{#language_ja}}
                以下のprivate.xmlの&lt;vendorid&gt;と&lt;productid&gt;を変更して使ってください。<br />
                （<a href="document.html.{{language}}#privatexml">private.xmlの使い方</a>）
              {{/language_ja}}
            </p>
          </li>
        </ol>

        {{#indented_xml}}
          |<?xml version="1.0"?>
          |<root>
          |
          |  <devicevendordef>
          |    <vendorname>My_Keyboard_VendorID</vendorname>
          |    <!-- *** Replace this value with yours. *** -->
          |    <vendorid>0x05ac</vendorid>
          |  </devicevendordef>
          |
          |  <deviceproductdef>
          |    <productname>My_Target_Keyboard_ProductID</productname>
          |    <!-- *** Replace this value with yours. *** -->
          |    <productid>0x0245</productid>
          |  </deviceproductdef>
          |
          |  <item>
          |    <name>Change f19 key to delete key</name>
          |    <appendix>(Change f19 key to caps lock key in internal keyboard.)</appendix>
          |    <identifier>private.f19_hack</identifier>
          |    <block>
          |      <!-- change f19 key to delete key -->
          |      <device_only>
          |        DeviceVendor::My_Keyboard_VendorID,
          |        DeviceProduct::My_Target_Keyboard_ProductID
          |      </device_only>
          |      <autogen>__KeyToKey__ KeyCode::F19, KeyCode::DELETE</autogen>
          |    </block>
          |    <block>
          |      <!-- change f19 key to caps lock key in other devices -->
          |      <autogen>__KeyToKey__ KeyCode::F19, KeyCode::CAPSLOCK</autogen>
          |    </block>
          |  </item>
          |
          |</root>
        {{/indented_xml}}


        <h2 id="beta-os-x">
          {{#language_en}}
            Seil does not work on developer-only beta of OS X. Why?
          {{/language_en}}
          {{#language_ja}}
            Seilが開発者向けのベータ版のOS Xで動作しません。なぜですか？
          {{/language_ja}}
        </h2>
        <p>
          {{#language_en}}
            We need to modify secret values of OS X to performs purpose of Seil.
          {{/language_en}}
          {{#language_ja}}
            Seilの機能を実現させるにはOS Xの隠された値を変更する必要があります。
          {{/language_ja}}
        </p>
        <p>
          {{#language_en}}
            However, we cannot access those secret values via SDK which Apple provides.
            We need <a href="http://opensource.apple.com/">source code of OS X</a> to modify these values.
          {{/language_en}}
          {{#language_ja}}
            しかし、Appleが提供するSDKからは隠された値にアクセスすることはできません。これらの値を変更するには、<a href="http://opensource.apple.com/">OS Xのソースコード</a>が必要です。
          {{/language_ja}}
        </p>
        <p>
          {{#language_en}}
            This source code will not be published until new OS X is shipped.
            Therefore, Seil is intentionally disabled on beta version of OS X.
          {{/language_en}}
          {{#language_ja}}
            このソースコードは新しいOS Xがリリースされるまでは公開されません。そのため、Seilは意図的にベータ版のOS Xでは動かないようにしています。
          {{/language_ja}}
        </p>
        <p>
          {{#language_en}}
            Please wait until new OS X is shipped from Apple.
            And please wait new version of Seil for new OS X.
          {{/language_en}}
          {{#language_ja}}
            Appleから新しいOS Xがリリースされ、新しいOS X向けのSeilがリリースされるまでお待ちください。
          {{/language_ja}}
        </p>
      </section>

      <section>
        <div class="page-header">
          <h1 id="commandlineinterface">
            {{#language_en}}
              Command line interface
            {{/language_en}}
            {{#language_ja}}
              コマンドラインインターフェース
            {{/language_ja}}
          </h1>
        </div>

        <p>
          {{#language_en}}
            Seil provides a command line interface. (seil command)
          {{/language_en}}
          {{#language_ja}}
            Seilはコマンドラインインターフェースを提供しています。(seilコマンド)
          {{/language_ja}}
        </p>
        <ul>
          <li>
            {{#language_en}}
              You can change settings.
            {{/language_en}}
            {{#language_ja}}
              設定を変更できます。
            {{/language_ja}}
          </li>
          <li>
            {{#language_en}}
              You can export configuration.
            {{/language_en}}
            {{#language_ja}}
              設定のエクスポートが出来ます。
            {{/language_ja}}
          </li>
        </ul>

        <h2>
          {{#language_en}}
            How to show help
          {{/language_en}}
          {{#language_ja}}
            使い方の表示方法
          {{/language_ja}}
        </h2>
        <p>
          {{#language_en}}
            Execute seil with no argument.
          {{/language_en}}
          {{#language_ja}}
            seilを引数なしで実行します。
          {{/language_ja}}
        </p>
        <pre>{{#strip_indented_lines}}
          |$ /Applications/Seil.app/Contents/Library/bin/seil
          |Usage:
          |  seil export
          |  seil relaunch
          |  seil set IDENTIFIER VALUE
          |
          |Example:
          |  seil export
          |  seil relaunch
          |  seil set keycode_capslock 80
        {{/strip_indented_lines}}</pre>

        <h2 id="export">
          {{#language_en}}
            About "export" option
          {{/language_en}}
          {{#language_ja}}
            exportオプションについて
          {{/language_ja}}
        </h2>
        <ol>
          <li>
            {{#language_en}}
              Export configuration by "export" option.
              <br />
              The output is a shell script.
            {{/language_en}}
            {{#language_ja}}
              exportオプションで設定のエクスポートを行います。
              <br />
              出力は、シェルスクリプトです。
            {{/language_ja}}
            <pre>{{#strip_indented_lines}}
              |$ /Applications/Seil.app/Contents/Library/bin/seil export > ~/Desktop/seil-import.sh
            {{/strip_indented_lines}}</pre>
          </li>
          <li>
            {{#language_en}}
              You can import it from Terminal.
            {{/language_en}}
            {{#language_ja}}
              ターミナルからインポートを行うことができます。
            {{/language_ja}}
            <pre>{{#strip_indented_lines}}
              |$ sh ~/Desktop/seil-import.sh
              |...........................................
            {{/strip_indented_lines}}</pre>
          </li>
        </ol>
      </section>

      <section>
        <div class="page-header">
          <h1 id="history">
            {{#language_en}}
              Version History
            {{/language_en}}
            {{#language_ja}}
              更新履歴
            {{/language_ja}}
          </h1>
        </div>

        {{#histories}}
          <h2>
            Version {{version}}
            {{#updated}}
              <small>
                {{#format_date}}
                  {{updated}}
                {{/format_date}}
              </small>
            {{/updated}}

            {{#link}}
              <a class="btn btn-default" href="{{link}}">
                {{#language_en}}
                  Download
                {{/language_en}}
                {{#language_ja}}
                  ダウンロード
                {{/language_ja}}
              </a>
            {{/link}}
          </h2>
          {{{body_l10n}}}
        {{/histories}}
      </section>
    </div>
    <div class="col-md-3">
      {{> ../../pqrs_tabs }}
    </div>
  </div>
</div>
